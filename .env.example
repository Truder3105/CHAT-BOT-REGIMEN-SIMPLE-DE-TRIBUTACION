# Google AI Studio — Generative Language API (referencia para un futuro backend proxy)
# El frontend actual NO lee este archivo; usa config.local.js (ver README).

GEMINI_API_KEY=

# Opcional: modelo de texto (el navegador elige automáticamente si lo dejas vacío)
GEMINI_MODEL=

ENVIRONMENT=development
