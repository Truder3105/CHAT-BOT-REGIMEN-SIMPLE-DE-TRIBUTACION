/** Configuración por defecto (sin clave). Para pruebas locales crea `config.local.js` (ver README). */
window.__RST_CONFIG__ = {
  GEMINI_API_KEY: "",
  /** Opcional: solo si conoces un id exacto de modelo de TEXTO en tu cuenta (no uses modelos *-tts). */
  GEMINI_MODEL: "",
};
